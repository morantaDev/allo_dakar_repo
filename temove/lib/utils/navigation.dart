import 'package:flutter/material.dart';

/// Clé globale pour le Navigator (pour éviter les erreurs de navigation après hot reload)
final GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();

